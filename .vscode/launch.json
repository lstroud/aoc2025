{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: uv run",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}",
            "python": "${workspaceFolder}/.venv/bin/python"
        }
    ]
}